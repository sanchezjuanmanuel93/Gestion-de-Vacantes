<a href="{{ $routeName }}" class="btn {{ $className }} btn-icon-split">
    <span class="icon text-white-50">
      <i class="fas {{ $iconName }}"></i>
    </span>
    <span class="text">{{ $displayName }}</span>
</a>